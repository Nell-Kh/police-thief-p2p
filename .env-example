# Secret placeholders - copy to .env and fill in real values. NEVER commit .env.
# Anthropic API key for the verbal-layer LLM provider (claude_api mode, Haiku)
ANTHROPIC_API_KEY=sk-ant-your-key-here
# Gmail OAuth files are file-based secrets (credentials.json / token.json),
# both git-ignored; see docs/PRD_reporting_gatekeeper.md and Appendix A.
