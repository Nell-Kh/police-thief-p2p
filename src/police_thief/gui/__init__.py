"""The GUI layer: live window and replay viewer - rendering only.

Everything these windows display is computed in the domain layer, which is
where the tests live; the GUI is excluded from coverage per the guidelines.
"""
